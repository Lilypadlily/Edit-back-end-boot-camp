//Just Until Day 7 Boot Camp
//async function routes(fastify, options) {
//    fastify.get("/awal", async (request, reply) => {
//      reply.send({ hello: request.query.nama || "orang asing" });
//  });
  
  //    fastify.get("/table/create", async (req, reply) => {
    //    const client = await fastify.pg.connect();
      //  const returnVal = await client.query(
        //  `CREATE TABLE ${req.query.name} (
          //  id int,
            //filter varchar(255),
           // imgSrc varchar(255),
            //title varchar(255),
            //summary varchar(255),
            //galleryHref varchar(255),
            //galleryTitle varchar(255)
      //  );`,
        //  [req.query.name]
       // )
       // client.release()
       // return returnVal;
//      })
    //}
   // 
 //   module.exports = routes